<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.greentea.mtspayandroidapplication">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/mts_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AuthenticationActivity"/>
        <activity android:name=".MenuActivity"/>
        <activity android:name=".PersonalAccountActivity"
                  android:label="@string/personalAccount"/>
        <activity android:name=".CameraQrReaderActivity"
              android:screenOrientation="portrait"/>
        <activity android:name=".PromptQrActivity"/>
        <activity android:name=".TransactionsActivity"
                  android:label="@string/transactions"/>
        <activity android:name=".TechnicalSupportActivity"
                  android:label="@string/technicalSupport" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>