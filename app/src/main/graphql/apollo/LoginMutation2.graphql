mutation LoginMutation2($email: String!, $password: String!) {

    login(
        email: $email,
        password: $password
    ) {
        jwt
        person {
            ...personData
        }
    }
}