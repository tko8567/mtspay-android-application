query FindPersonById ($id: String){
  person(id: $id) {
    id
    firstName
    lastName
    cards {
      id
      number
      validThru
      balance
      isDefault
    }
    transactions {
      id
      from {
        firstName
        lastName
      }
      to {
		... on Person {
          firstName
          lastName
        }
        ... on Organization {
          name
        }
      }
      amount
      description
      status
    }
  }
}

